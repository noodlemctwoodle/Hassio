  email: !secret google_username
  password: !secret google_password
  intents:
    HassLightSet:
      sentences:
        - Toggle {name}.
      conversations:
        - id: !secret notifier_user_one_id
    Ping:
      sentences:
        - How many Conversation do you know
  error_suppressed_conversations:
    - id: !secret notifier_user_one_id